---
description: 通义灵码规则兼容 - 读取并执行.lingma/rules下的规则文件
globs: 
alwaysApply: true
---

# Lingma Rules Compatibility

## 规则

项目根目录下 `.lingma/rules/` 文件夹中存放通义灵码的规则文件。在执行任务前，必须检查该目录是否存在，若存在则读取其中所有规则文件并遵照执行。

## 执行流程

1. 检查项目根目录下是否存在 `.lingma/rules/` 目录
2. 若存在，读取该目录下所有文件
3. 将读取到的规则内容作为补充约束，与 `.cursor/rules/` 中的规则共同生效
4. 若两套规则冲突，以 `.cursor/rules/` 为准

## 说明

此规则用于保持 Cursor 与通义灵码的规则兼容性，使同一项目可以同时被两种 AI 编码工具使用相同的规范。
