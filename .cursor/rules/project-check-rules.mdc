---
description: 项目结构与文档检查规则 - 确保项目包含必要的文档结构和版本信息
globs: 
alwaysApply: true
---

# Project Structure Check Rules

## 文档目录规范

1. **文档集中管理**: 除 `README.md` 外，所有文档放在项目根目录的 `a-doc/` 文件夹中
2. **README.md 保留在根目录**: `README.md` 是项目总览入口，必须在根目录
3. **禁止根目录散落文档**: 根目录不应存在除 `README.md` 以外的 `.md` 文件

## 必需文档清单

| 文件 | 位置 | 说明 |
|------|------|------|
| `README.md` | 根目录 | 项目总览，简要说明项目用途、规则列表、使用方式 |
| `install.md` | `a-doc/` | 运行/安装说明 |
| `version.md` | `a-doc/` | 版本迭代记录，格式为 `## vX.Y.Z (YYYY-MM-DD)` + 变更列表 |

## 自动生成规则

- 如果项目缺少 `a-doc/version.md`，应自动生成初始版本记录
- 如果项目缺少 `a-doc/install.md`，应自动生成基础运行说明模板

## version.md 格式规范

```markdown
# 版本记录

## vX.Y.Z (YYYY-MM-DD)

- 新增：xxx
- 修复：xxx
- 调整：xxx
```

## 文档写作要求

1. **简洁明朗**: 避免冗余描述，用表格和列表代替大段文字
2. **结构清晰**: 使用标题层级组织内容，每个章节职责单一
3. **可操作性**: 说明文档应包含可直接执行的步骤或命令
4. **保持更新**: 版本发布时同步更新 `version.md`
